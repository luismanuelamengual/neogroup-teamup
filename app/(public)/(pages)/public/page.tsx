import LandingPage from '@/app/components/LandingPage'

/** Preview route for the root-domain landing page. Useful for local development. */
export default function LandingPreviewPage() {
  return <LandingPage />
}
