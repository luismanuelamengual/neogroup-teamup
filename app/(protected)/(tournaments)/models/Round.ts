/**
 * @deprecated The `rounds` table has been removed. A tournament's structure now
 * lives entirely on `matches` (roundNumber, type, groupNumber, position,
 * nextMatchId). Use the `Match` model instead.
 */
export {}
