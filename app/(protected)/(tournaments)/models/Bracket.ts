/**
 * @deprecated The free-text `rounds.bracket` discriminator has been replaced by
 * the (rounds.type, rounds.groupNumber) pair. Use `RoundType` (and
 * `groupNumber` for the group index) instead.
 */
export {}
