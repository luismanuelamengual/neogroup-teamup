/**
 * @deprecated The free-text `rounds.bracket` discriminator has been replaced by
 * rounds.type + rounds.settings.groupNumber. Use `RoundType` (and
 * `settings.groupNumber` for the group index) instead.
 */
export {}
