/** Configurable settings of playoff (knockout) tournaments. */
export interface PlayoffSettings {
  // No configurable settings — tournament-type variants (PLAYOFF vs
  // PLAYOFF_WITH_CONSOLATION) encode structural differences.
}

export const DEFAULT_PLAYOFF_SETTINGS: PlayoffSettings = {}
